{
  "name": "datastructure",
  "version": "0.0.0",
  "description": "Datastructure in javascript",
  "main": "main.js",
  "scripts": {
    "build": "babel lib/ -d dist",
    "test": "npm run build && ./node_modules/.bin/mocha --require @babel/register 'test/**/*.js' --grep='Search unique'",
    "launch": "npm run build && node dist/main.js"
  },
  "author": "Sylvain Abelard",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "@babel/register": "^7.6.2",
    "mocha": "^6.2.2"
  }
}
